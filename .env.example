# Core API Configuration / 核心 API 配置
# ------------------------------------

# (Required) Pixiv Refresh Token for API authentication
# (必填) 用于 Pixiv API 认证的 Refresh Token
PIXIV_REFRESH_TOKEN=

# (Optional) Database URL
# Default: file:./core.db
# (可选) 数据库连接字符串
# 默认值: file:./core.db
API_DB_URL=file:./core_data.db

# (Optional) Scheduler polling interval in seconds
# Default: 900 (15 minutes)
# (可选) 轮询间隔（秒）
# 默认值: 900 (15分钟)
POLL_INTERVAL=900

# (Optional) Logging level
# Values: debug, info, warn, error
# Default: info
# (可选) 日志级别
# 可选值: debug, info, warn, error
# 默认值: info
LOG_LEVEL=info

# (Optional) Enable debug mode
# Default: false
# (可选) 启用调试模式
# 默认值: false
DEBUG=false

# (Optional) Enable verbose request logging
# Default: false
# (可选) 启用详细请求日志
# 默认值: false
VERBOSE_REQUEST=false

# (Optional) URL for the Bot Webhook (used by Core API to push updates)
# Default: http://localhost:3001/webhook
# (可选) Bot Webhook URL (Core API 用于推送更新)
# 默认值: http://localhost:3001/webhook
BOT_WEBHOOK_URL=http://localhost:3001/webhook

# Bot Service Configuration / Bot 服务配置
# --------------------------------------

# (Required) Telegram Bot Token
# (必填) Telegram Bot Token
BOT_TOKEN=

# (Optional) Database URL for Bot Service
# Default: file:./bot.db
# (可选) Bot 服务数据库连接字符串
# 默认值: file:./bot.db
BOT_DB_URL=file:./bot_data.db

# (Optional) URL of the Core API service
# Default: http://localhost:3000
# (可选) Core API 服务地址
# 默认值: http://localhost:3000
CORE_API_URL=http://localhost:3000

# (Optional) Port for the Bot Webhook server
# Default: 3001
# (可选) Bot Webhook 服务端口
# 默认值: 3001
WEBHOOK_PORT=3001

# Proxy Configuration (Optional) / 代理配置 (可选)
# ---------------------------------------------

# Used if you need to access Pixiv/Telegram through a proxy
# 如果你需要通过代理访问 Pixiv 或 Telegram，请设置以下变量

# HTTP_PROXY=http://127.0.0.1:7890
# HTTPS_PROXY=http://127.0.0.1:7890
# ALL_PROXY=socks5://127.0.0.1:7890
